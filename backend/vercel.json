{
    "version": 2,
    "routes": [
      {
        "src": "/api/(.*)",
        "dest": "/api/$1"
      }
    ],
    "builds": [
      {
        "src": "server/app.js",
        "use": "@vercel/node"
      }
    ]
  }